[package]
name = "components"
version = "0.1.0"
edition = "2024"

[dependencies]
dioxus = { workspace = true }
kernel = { workspace = true }

strum.workspace = true

[features]
server = ["kernel/server"]
